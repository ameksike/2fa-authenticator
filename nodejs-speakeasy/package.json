{
  "name": "nodejs-speakeasy",
  "version": "1.0.0",
  "description": "Two-factor authentication (also known as 2FA) is a type, or subset, of multi-factor authentication.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch src src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antonio Membrides Espinosa",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "qrcode": "^1.4.4",
    "speakeasy": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
